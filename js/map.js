function initMap(){const el=document.getElementById("map");if(!el||!window.L){window.addEventListener("load",initMap,{once:true});return}const map=L.map(el).setView([25.9,85.3],7);L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png",{attribution:"© OpenStreetMap"}).addTo(map);const layer=L.layerGroup().addTo(map);window.addEventListener("ks:markers",e=>{layer.clearLayers();e.detail.forEach(x=>{if(x.lat&&x.lng)L.marker([x.lat,x.lng]).addTo(layer).bindPopup(`<b>${escapeHTML(x.name)}</b><br>${formatCurrency(x.priceHourly)}/hr<br><a href="booking.html?id=${x.id}">Book Now</a>`)});if(e.detail.length)map.fitBounds(e.detail.filter(x=>x.lat).map(x=>[x.lat,x.lng]),{padding:[20,20]})});setTimeout(()=>map.invalidateSize(),300)}