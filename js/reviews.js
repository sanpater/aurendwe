function addReview(equipmentId,rating,text){const a=getReviews();a.push({id:generateId("R"),equipmentId,farmerId:getCurrentUser()?.id,rating,text,createdAt:new Date().toISOString()});saveReviews(a);const eq=getEquipment(),e=eq.find(x=>x.id===equipmentId);if(e){const rs=a.filter(x=>x.equipmentId===equipmentId);e.rating=Number((rs.reduce((s,x)=>s+x.rating,0)/rs.length).toFixed(1));e.reviews=rs.length;saveEquipment(eq)}}