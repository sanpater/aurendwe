function initSupport(){const f=document.getElementById("complaintForm");if(!f)return;f.addEventListener("submit",e=>{e.preventDefault();const a=getComplaints();const u=getCurrentUser();a.push({id:generateId("C"),bookingId:complaintBooking.value,userId:u?.id||"U001",category:complaintCategory.value,description:complaintDescription.value,photo:complaintPhoto.files[0]?.name||"",date:Date.now(),status:"Open"});saveComplaints(a);showToast("Complaint submitted");f.reset()})}